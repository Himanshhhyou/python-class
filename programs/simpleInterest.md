## Simple Interest
``` python
p = int(input("Enter the principle amount: "))
r = int(input("Enter the rate(in %): "))
t = int(input("Enter the time (in years): "))
si = (p*r*t)/100
print("Simple interest is " + str(si) +" rs.")
```